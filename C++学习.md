# 第一个程序的书写和运行

```c++
#include<iostream>
using namespace std;

int mani()
{
	cout <<"hello world"<< endl;
	return 0;
}
```

## 1.标准函数库的调用 `#include`
```c++
#include<iostream>
```
此部分代码表示引入一个函数库进行使用，此处使用的是C++语言特有的函数库“iostream”，C++语言同时还兼容一部分C语言的函数库，但是后缀都是带有".h",比如`<stdio.h>`,这是过去一部分旧的写法，而新的C++语言中就会没有这样的后缀`<iostream>`。
## 2.命名空间的使用 `namespace`
```c++
using namespace std;
```
这一串代码意味着使用标准函数库的命名空间，由于外部的（自行创作的）函数名可能和系统的函数名重合，所以就划分了很多个命名空间，来避免一个函数名可能运行了很多不同的函数主体这种情况。
其中`std`，代表了标准函数库的命名空间。
使用上文的方法后，会使得后面的代码函数全在`std`这种函数命名空间去找，这种办法是不妥当的。
那有没有办法来单独使用一个在某个命名空间的函数呢？
```c++
std::cout <<"hello world" <<std:: endl;
```
在需要运行的指令前面加上`std::`即可，在冒号面前加上相应的命名空间即可。
## 3.cin 和cout 作用与区别
`cin`：起到类似c语言中`scanf`的作用，但是无需再书写对应的数据格式，它会自行判断
`cout`：起到类似c语言中`printf`的作用，但是无需书写对应的数据格式，会自行判断。

```c++
int a;
cin >> a;
cout << a <<endl;
```

如果输入a=1.23，此时输出会以a的数据类型进行自动输出1
!![image-2026192852454.png](/C++%E5%AD%A6%E4%B9%A0.assets/image-2026192852454.png)